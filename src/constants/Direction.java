package constants;

/**
 * The Direction enumeration holds values for the direction an entity can be facing in
 * Is used to set the xDirection for an entity, including player and enemy
 */
public enum Direction {
    LEFT, RIGHT, STILL, UP, DOWN
}